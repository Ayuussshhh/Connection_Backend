# SchemaFlow - Interactive Database Platform Configuration
# =========================================================

# Server Configuration
HOST=127.0.0.1
PORT=3000

# Logging
RUST_LOG=info,interactive_db_api=debug,tower_http=debug

# CORS - Add your frontend URL here
ALLOWED_ORIGINS=http://localhost:3001,http://127.0.0.1:3001

# =========================================================
# DATABASE CONFIGURATION (OPTIONAL!)
# =========================================================
# You can now connect to databases dynamically via the API!
# Use: POST /api/connections with a connection string
#
# The settings below are OPTIONAL and only needed if you want
# to use the legacy /db/* endpoints for database management.
# =========================================================

# PostgreSQL Configuration (Optional - for legacy mode)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=your_password_here
# DB_NAME=postgres
# DB_MAX_POOL_SIZE=10
